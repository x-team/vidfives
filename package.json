{
  "name": "vidfives",
  "version": "1.0.0",
  "description": "tell someone they're awesome",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node .",
    "clean": "rm -fR dist",
    "prebuild": "npm run clean && mkdir -p dist",
    "build": "cp -R src/assets/* dist/ && npm run build-js",
    "build-js": "browserify -t babelify -p [ css-modulesify -o dist/main-home.css ] -o dist/main-home.js src/main-home.js",
    "lint": "eslint src/ routes/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joshwnj",
  "license": "MIT",
  "dependencies": {
    "ecstatic": "^0.8.0",
    "formidable": "^1.0.17",
    "http-hash-router": "^1.1.0",
    "hyperscript": "^1.4.6",
    "react": "^0.13.3",
    "slack-node": "^0.1.3"
  },
  "devDependencies": {
    "babelify": "^6.1.2",
    "browserify": "^10.2.6",
    "css-modulesify": "^0.3.3",
    "eslint": "^0.24.1",
    "eslint-plugin-react": "^2.7.0"
  }
}
